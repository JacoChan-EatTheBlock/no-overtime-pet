# Lessons Learned

## 记录格式

每条记录包含：

- 日期
- 问题现象
- 导致原因
- 修正内容
- 防止规则
- 关联文件
- 验证方式

---

## 2026-08-29：动画生成管线不能成为产品运行时依赖

### 问题现象

早期技术讨论可能让人误以为需要把已有角色生成和视频处理管线直接接入桌宠产品。

### 导致原因

没有及时区分“运行时产品能力”和“离线美术资源生产方式”。

### 修正内容

产品只加载已经产出的预制角色、Sprite Sheet 和动画 Manifest。外部生成、提帧、去背景和质检流程只负责制作资源。

### 防止规则

任何运行时架构、安装包或服务依赖中都不得包含角色视频生成管线；除非 Magnus 后续明确改变预制角色策略。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`

### 验证方式

- 检查运行时依赖中没有 Seedream、Seedance、FFmpeg 动画生成任务或按用户生成角色的接口。
- 检查客户端只通过 Manifest 加载通过 QA 的预制资产。

---

## 2026-08-29：桌面活动识别优先准确度

### 问题现象

初版方案过早围绕视觉模型调用费用压缩识别频率，可能牺牲状态准确度。

### 导致原因

把成本优化放在了产品可信度之前。

### 修正内容

识别综合使用前台应用、窗口上下文、浏览器页面、输入活动、截图和在线视觉模型；调用成本暂不作为首要约束。

### 防止规则

优化识别成本前必须先证明准确度、状态稳定和敏感信息边界不退化。不确定时显示中性状态，不得直接公开判定用户摸鱼。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`

### 验证方式

- 使用覆盖写作、编程、会议、查资料、视频、聊天、锁屏和混合场景的标注样本评估。
- 单独统计错误公开为“摸鱼”的误报率。

---

## 2026-08-29：用户决定必须高于 AI 排程

### 问题现象

如果 AI 可以覆盖用户填写的 DDL、重要性、时长或手动顺序，自动排程会失去可信度。

### 导致原因

没有将正式事实、AI 建议和用户手动约束明确分层。

### 修正内容

用户填写 DDL 和重要性；AI 只建议任务类型、时长、认知负荷、拆分方式和时间偏好。任何用户修改都会成为本轮排程的更高优先级约束。

### 防止规则

AI 不得创建或修改 DDL、重要性、完成状态和用户锁定时间。自动重排只能影响尚未开始、尚未完成且未锁定的未来时间块。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`

### 验证方式

- 构造 AI 建议与用户约束冲突的测试，确认最终计划始终服从用户。
- 确认无解时系统说明冲突，而不是篡改事实或压缩时长。

---

## 2026-08-29：窝囊费不能依赖桌面状态推断

### 问题现象

早期讨论为窝囊费增加了离岗、摸鱼和系统识别等不必要判断。

### 导致原因

把角色状态识别和工资时间计算混成了同一个系统。

### 修正内容

窝囊费只使用用户输入的日薪、上班时间、下班时间和午休时间。桌面识别只负责角色动作和好友状态展示。

### 防止规则

活动分类、截图结果、键盘事件和任务完成度均不得改变窝囊费计算。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`

### 验证方式

- 对相同工作时间设置输入不同桌面活动，确认窝囊费曲线完全相同。

---

## 2026-08-29：联机场景不应膨胀成复杂虚拟办公室

### 问题现象

“虚拟办公室”容易被理解为需要场景探索、走动、房间装修和复杂多人游戏能力。

### 导致原因

视觉包装没有与产品核心玩法分开。

### 修正内容

首版只实现好友角色排排坐着工作、动作同步和泛化状态展示；装饰消费集中在角色与可叠高帽子上。

### 防止规则

未经 Magnus 明确批准，不增加自由移动、寻路、地图、房间建造、家具摆放或物理系统。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`

### 验证方式

- 审查 PRD、视觉稿和任务拆分，确认复杂办公室能力不在首版范围。

---

## 2026-08-29：键盘动作不能变成键盘记录器或网络洪泛

### 问题现象

类似 Bongo Cat 的拍键盘动作需要响应输入，但错误实现可能记录具体按键，或把每次按键同步给服务器。

### 导致原因

没有区分本地即时动画输入和联机聚合状态。

### 修正内容

本地只消费“发生键盘活动”的事件来交替播放左右手动作；联机只发送 typing start、heartbeat、stop 或等价聚合状态。

### 防止规则

不得持久化、记录、输出日志或联网传输具体按键值、组合键内容和用户输入文本。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`

### 验证方式

- 安全测试确认 IPC、日志、数据库和网络请求中均没有按键值。
- 高频输入压力测试确认网络事件保持限频。

---

## 2026-08-29：[已废止] 窝囊费与窝囊时长必须是两套独立数值

> **[已废止]** 本条记录保留用于说明错误方案，不得作为当前实现依据。Magnus 随后确认用户只应感知一种货币，当前规则见下一条“唯一货币必须使用时间归一化定价”。

### 问题现象

如果直接按照用户填写的日薪兑换装饰货币，用户可以恶意填写超高工资，立即获得大量角色和帽子，破坏游戏内经济与装饰稀缺感。

### 导致原因

把用于用户情绪感知的真实工资计数，与游戏内可消费资源错误地当成同一种资产。

### 修正内容

窝囊费只按用户输入的日薪显示，不参与任何购买；窝囊时长按可计入的工作时长独立累计。角色和帽子统一按分钟或小时标价，只能用窝囊时长购买。

### 防止规则

- 日薪不得进入窝囊时长余额、商品价格、购买资格或奖励倍率的计算。
- 窝囊费与窝囊时长使用不同字段、不同账本和不同界面语义。
- 首版不开放玩家间货币或装饰交易，避免形成可被多账号或异常输入操纵的市场。
- 商品价格只以时长单位配置，禁止同时存在隐式工资汇率。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`

### 验证方式

- 分别输入低日薪和极高日薪，在相同可计入工作时长下获得完全相同的窝囊时长。
- 修改日薪后，商品价格、余额和购买结果均保持不变。
- 购买只产生窝囊时长账本扣减，不读取窝囊费字段。

---

## 2026-08-29：唯一货币必须使用时间归一化定价

### 问题现象

把窝囊费仅作为展示、再增加一个可消费的窝囊时长钱包，会让用户同时理解两套高度相似的数值，不符合“窝囊费是唯一货币”的产品方向。

### 导致原因

为防止高日薪用户获得过强购买力，错误地把后台归一化单位直接暴露成第二种货币，而没有将价格也按同一工资率缩放。

### 修正内容

用户只看到和使用窝囊费。角色和帽子配置固定的价格窝囊时长；用户实际看到的价格为 `单位时间窝囊费 × 价格窝囊时长`。后台以等价工作毫秒记录余额和扣减，界面再按同一单位时间窝囊费换算余额与价格。

### 防止规则

- 后台等价毫秒只是账本尺度，不得显示为第二个钱包或第二种货币。
- 商品权威基础价格必须是 `requiredWorkMs`，不能只存某个用户币种下的固定金额。
- 日薪、币种或标准工作时长变化时，余额显示和商品价格必须同比变化；`balanceEquivalentMs / requiredWorkMs` 的购买能力保持不变。
- 购买事务扣减等价毫秒，同时记录当时显示金额快照用于对账。
- 不允许先用高工资积累固定金额、再降低工资购买低价商品的套利路径。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`

### 验证方式

- 两名工资不同的用户购买同一商品都需要积累相同有效工作时长。
- 同一用户修改日薪前后，余额金额和商品金额同比变化，可购买的商品数量不变。
- 数据库中不存在可独立消费的第二个“窝囊时长余额”。

---

## 2026-08-29：[已废止] 加班池按等价时间分配

> **[已废止]** 本条保留用于解释上一版方案。当前奖励池不再按等价时间均分，最新规则见下一条“奖励池必须按人民币原额分配”。

### 问题现象

如果加班倒扣的窝囊费直接消失，它只有惩罚感，没有形成“不要加班”的多人正反馈；如果直接按不同用户的货币金额转账，又会因为工资率不同破坏购买力公平。

### 导致原因

早期经济模型只描述了加班扣减，没有定义扣减后的去向和跨工资用户的结算尺度。

### 修正内容

加班者被扣除的后台等价工作时间进入每日奖励池。完成当日承诺待办并准点跑路的合格用户领取奖励；多人合格时平均分配等价时间，再分别按接收者自己的单位时间窝囊费显示到账金额。

### 防止规则

- 奖励池必须以后台归一化时间计量，不直接汇总不同币种和工资率的显示金额。
- 每次加班扣减只能生成一条幂等的池入账记录。
- 每个结算周期只能生成一次分配结果；收款账本需要唯一的 `settlementId + recipientId`。
- 合格资格必须基于冻结的当日承诺任务快照和明确的准点截止时间，不能通过结算前删除任务取得资格。
- 整除余数必须进入下一期池或按确定性规则处理，不能因浮点误差丢失或重复生成。

### 关联文件

- `AGENTS.md`
- `docs/prd/10-economy-shop-customization.md`
- `docs/prd/13-api-event-contracts.md`
- `docs/prd/14-data-dictionary.md`

### 验证方式

- 单个合格用户获得全部池内等价时间。
- 多个合格用户获得相同份额，份额总和与池总额及余数一致。
- 不同工资率用户领取同一份额后，购买力增加相同，显示金额按各自工资率不同。
- 重复运行结算任务不会重复扣款或重复发奖。

---

## 2026-08-29：奖励池必须按人民币原额分配

### 问题现象

把所有加班贡献先换算成等价时间再均分，会抹掉工资差异：高工资用户加班实际被扣掉更多人民币窝囊费，却不会给低工资获奖者带来额外福利，不符合最新玩法判断。

### 导致原因

上一版把“防止用户修改日薪套利”的个人钱包规则错误扩展到公共奖励池，过度追求所有用户购买力相同，忽略了高工资用户加班惠及低工资用户本身可以是合理、可感知的联机奖励。

### 修正内容

首版只处理人民币。贡献者加班实际减少多少人民币窝囊费，奖励池就按原额增加多少人民币分；多人合格时按人民币分平均。低工资用户领取同额人民币奖励后获得更多商店购买力，这是预期结果。

奖励入个人钱包时仍按收款人结算时费率转换为规范购买力并保存费率快照。该个人会计转换不改变池内金额，只用于保证收款人之后修改日薪时不会再次放大奖励。

### 防止规则

- `OvertimePoolContribution.amountMinor` 是池贡献事实，币种固定 `CNY`。
- `sourceForfeitedEquivalentMs` 只用于核对贡献来源，不得参与池汇总和均分。
- 贡献金额使用累计扣款金额差计算，避免分段计提造成舍入差异。
- 多人领取时计算 `floor(distributableMinor / eligibleCount)`，人民币分余数结转。
- 池守恒以人民币分验证：贡献加结转必须等于发放加余数。
- 收款账本同时保存 `awardedMinor`、`creditedEquivalentMs` 和收款人费率快照。
- 禁止重新引入 `equalShareEquivalentMs` 等池内等价时间分配字段。

### 关联文件

- `AGENTS.md`
- `docs/prd-combined-v1.0.md`
- `docs/prd/01-shared-contracts.md`
- `docs/prd/03-work-settings-nang-fee.md`
- `docs/prd/13-api-event-contracts.md`
- `docs/prd/14-data-dictionary.md`
- `docs/prd/15-test-acceptance.md`
- `docs/prd/datasets/economy-settlement-scenarios.example.json`

### 验证方式

- 每小时 100 元的用户加班 30 分钟，池实际增加 50 元，而不是“30 分钟”。
- 50 元/小时的用户单独领取这 50 元后，增加 1 小时商品购买力。
- 三名工资不同的获奖者从 60 元池中各领取 20 元，但获得的个人购买力不同。
- 1001 分由两人领取时各得 500 分，1 分结转。
- 收款人修改日薪后，已领取奖励的购买力不再二次放大。
