# 不要加班 Agent 规则

## 1. 项目简介

“不要加班”是一款 macOS 联机工作桌宠产品，目标是让用户把该完成的待办做完并准点下班。用户只负责录入任务名称、DDL 和重要性，系统分析任务类型、预计时长与认知负荷，再生成当天的可调整工作安排。

工作期间，预制像素角色根据键盘、鼠标、会议、浏览内容、离开工位等真实状态播放动作；已互加好友的用户可以在一个简单的“排排坐工作”界面中看到彼此的泛化状态与同步动作。产品当前处于 PRD 与技术方案阶段，尚未开始工程实现。

## 2. 技术栈

- Desktop Runtime: Electron
- UI Framework: React
- Language: TypeScript
- Build: Vite
- 2D Rendering: PixiJS
- Client State: Zustand
- Server State: TanStack Query
- Local Database: SQLite
- Cloud Database: PostgreSQL
- Server API: NestJS + TypeScript
- Auth & Database: 可使用 Supabase Auth 与托管 PostgreSQL，但经济、好友授权和结算仍由 NestJS 权威业务层执行
- Realtime & Cache: Socket.IO + Redis
- Object Storage: S3 兼容存储（可由 Supabase Storage 承载）
- Deterministic Scheduling: 独立的纯 TypeScript 调度核心
- AI Service: Python FastAPI + 可替换在线模型 Provider，只输出结构化建议
- Desktop Recognition: macOS 前台应用、输入活动、可选窗口上下文、屏幕截图与在线视觉模型组合
- Distribution: 中国大陆首发、Apple Silicon `arm64` 优先；Developer ID 签名、公证后的 DMG 官网分发
- Asset Production: 外部 Python、FFmpeg、Pillow、NumPy、Seedream/Seedance、Aseprite 管线
- Tests: Vitest、React Testing Library、Playwright Electron

依赖版本在工程初始化时锁定当时维护中的稳定版本，不在 PRD 阶段预写可能过时的精确版本号。

## 3. 计划目录结构

```text
apps/
  desktop/
    main/           Electron 主进程、macOS 能力、权限、输入与截图识别
      platform/     平台接口；首版实现 macOS，Windows 仅保留未来扩展边界
    preload/        最小权限 IPC Bridge
    renderer/       React 页面、PixiJS 桌宠与联机座位区
  api/              账号、好友、任务同步、实时网关、商店与账本 API
  ai-service/       FastAPI 任务分析、视觉适配和评估入口

packages/
  contracts/        IPC、HTTP、Realtime、AI 和动画 Manifest 契约
  domain/           无副作用的排程、活动映射与窝囊费规则
  asset-runtime/    Manifest 校验和 PixiJS 资源适配
  test-fixtures/    脱敏及合成测试数据

assets/
  characters/       预制角色运行时资产
  hats/             帽子运行时资产与锚点元数据
  manifests/        动作包 Manifest

docs/
  PRD 与架构文档

tests/
  跨模块契约和端到端测试
```

实际工程初始化后，应按真实目录同步更新本节，不允许让规则文件长期保留虚构路径。

## 4. 项目核心路线

- macOS 优先开发与首发；Windows 延后，不属于 MVP 发布承诺。
- 首版正式支持 Apple Silicon `arm64`；Intel Mac 暂不承诺。
- 首发通过官网分发 Developer ID 签名并经 Apple 公证的 DMG；Mac App Store 不进入 MVP。
- MVP 仅面向中国大陆，所有工作日与结算固定使用北京时间 `Asia/Shanghai`，不提供用户时区选择。
- 先完成“任务录入 → 自动排程 → 工作动作 → 好友可见 → 准点下班 → 装饰消费”的闭环。
- 用户决定始终高于 AI 建议。
- 识别准确度优先于在线视觉模型调用成本。
- 联机场景只需让好友角色排排坐着工作，不建设复杂可探索虚拟办公室。
- 先使用预制角色和动作包，不做用户运行时 AI 生成角色。
- 动画生成管线只用于离线资源生产，不成为产品运行时依赖。

## 5. 适配原则

- 首发平台：macOS 桌面端；开发基线暂定 macOS 13，工程锁定 Electron 版本后复核最低系统版本。
- 支持普通设置主窗口、菜单栏常驻入口和独立透明桌宠窗口。
- 桌宠定位必须依据当前显示器 `workArea`，适应 Dock 位于底部/左侧/右侧、自动隐藏、多显示器、Spaces、全屏应用和 Stage Manager。
- UI 必须适应 Retina、高 DPI、不同缩放与“减少动态效果/降低透明度”等系统辅助功能。
- 系统权限被拒绝时，任务、排程、窝囊费、联机和基础桌宠必须继续可用；只降级受影响的活动识别信号。
- 联机座位区以横向排列、自动换行或分页承载好友数量，不实现自由移动、寻路或场景探索。
- 像素资产使用整数倍或最近邻缩放，禁止模糊插值。
- 精确视口、桌宠尺寸、角色基准像素和座位容量在视觉方向确定后补齐。

## 6. 核心业务规则

### 6.1 用户与任务

- 用户录入任务时，必填任务名称、DDL 和重要性。
- 新建任务后自动触发 AI 分析并进入建议确认页；任务列表不得出现逐条“交给 AI 分析”按钮。
- AI 建议主操作统一为“确认建议”，只有用户确认后才写入接受或修改过的正式字段。
- 紧急程度由系统根据 DDL、预计剩余时长和可用工作时间计算，不要求用户重复填写。
- 用户填写或修改的时长、顺序、固定时间、锁定状态和拆分偏好始终覆盖 AI 建议。
- AI 不得创造或擅自修改 DDL、重要性、任务完成状态或用户锁定时间。
- 初次生成的今日安排必须让用户看到完整草案并确认；只有用户主动开启“软计划自动优化”后，系统才可自动调整未来未锁定时间块。
- AI 只能自动重排未开始、未完成且未锁定的未来时间块。

### 6.2 工作时间与窝囊费

- 用户自行输入人民币日薪、上班时间、下班时间和午休时间；首版只处理人民币，不提供币种或时区选择。
- 周末和法定节假日默认不工作，用户可覆盖具体日期。
- 系统不得根据桌面状态推断或修改这些时间。
- 只有 App 已打开并进入已登录可用状态后才开始计提；离线区间不计提。
- 工作时段内窝囊费按时间增加，午休暂停；超过下班时间仍未跑路时按同一速度倒扣。
- 桌面识别结果不影响窝囊费。
- 窝囊费是用户唯一感知和使用的游戏货币，首版金额单位固定为人民币。
- 商品不配置固定金额，而是配置固定的“价格窝囊时长”；用户实际价格为 `单位时间窝囊费 × 价格窝囊时长`。
- 用户通过自己的工作积累时，相同工作时间应获得相同购买力：日薪高时窝囊费累计更快，但角色和帽子的窝囊费价格也按同一比例升高。
- 后台统一使用等价毫秒作为归一化账本单位，但不得向用户展示第二个“窝囊时长钱包”。
- 用户修改日薪时，窝囊费余额展示与所有商品价格必须同比重算，购买力不得改变，不能形成先高薪积累再低薪购买的套利。
- 工作时段积累归一化毫秒，午休暂停；超过下班时间仍未跑路时按时间倒扣归一化毫秒，并同步反映为窝囊费下降。
- 加班实际倒扣的人民币窝囊费不得销毁，必须按原金额进入待结算奖励池；池内不得按工资率换算成统一工作时长。
- 闪退或断线时好友端角色置灰；结算前重连恢复资格并从重连时继续记录，结算时仍离线则不参与当日奖励结算，并把当日已产生的窝囊费从个人钱包原子扣回并转入奖励池。
- 完成当日承诺待办且准点跑路的合格用户分享奖励池；合格用户超过一人时按人民币最小单位“分”平均分配，余数结转。
- 中国大陆全体注册用户共用一个北京时间奖励池，不设置最小参与人数；单一玩家符合条件时可以独自领取。
- 无人符合时池余额全部滚存；每笔池金额从首次入池起保留 7 个北京时间自然日，到期销毁且不得因滚存刷新期限。
- 高工资用户同样加班时会贡献更多人民币窝囊费；低工资获奖者领取同额奖励后获得更多装饰购买力，这是预期福利。
- 奖励进入个人钱包时按收款人结算时费率固化为规范购买力，收款人以后修改日薪不得再次放大奖励。
- 窝囊费可购买预制角色和帽子；装饰不得提供排程或工作效率优势。
- 首版不支持玩家之间转移、赠送、出售或交易窝囊费与装饰，避免形成可套利的玩家市场。

### 6.3 桌面识别与展示

- 识别综合使用前台应用、窗口上下文、浏览器页面、输入活动、截图和在线视觉模型。
- 准确度优先于调用次数和单次识别成本。
- 对好友只展示泛化活动、角色动作和必要的在线状态。
- 不向好友传输原始截图、具体按键、文件名、窗口标题、聊天内容、任务正文或日薪。
- 不确定时显示中性状态，不得直接公开判定用户摸鱼。
- 用户必须能够暂停或关闭识别；识别结果只读，不提供活动纠正、类别改写或历史回写入口。
- 在线截图识别产品开关默认开启，但实际采集必须等待 macOS Screen Recording 授权；拒绝或撤销后立即降级。

### 6.4 桌宠动作与联机

- 键盘输入应低延迟触发类似 Bongo Cat 交互机制的左右手拍键盘动作。
- 只能使用输入事件类型和时间驱动动作，不得记录或传输具体按键值。
- 本地动作立即播放；联机只同步聚合后的动作状态，不逐次发送每个键盘事件。
- 好友端播放同一动作类型，并在状态超时、断线或用户跑路后安全回到待机或离开状态。
- 好友活动广播与本机好友桌宠显示必须是两个独立设置；不得用一个开关同时改变发送和显示。
- “不对其展示”只停止向指定好友发送自己的活动投影，好友关系保持，反向投影不受影响；MVP 不提供拉黑能力。
- Bongo Cat 只作为“输入映射到角色动作”的机制参考，不得未经授权复制其角色、美术、品牌或代码。

### 6.5 联机场景与装饰

- 虚拟办公室不是核心玩法，只需要好友角色排排坐着工作。
- 首版不做角色自由移动、房间建造、家具摆放、地图探索或物理碰撞。
- 角色和帽子均为预制资产。
- 用户可同时装备多顶帽子，帽子按顺序向上叠加，形成越来越高的帽子塔。
- 帽子玩法不设置固定的装备数量硬上限；超出可见高度后直接使用纵向滚动和静态层缓存，不自动缩小角色或帽子塔，不得静默丢弃已装备帽子。
- 帽子叠放必须使用资产元数据中的锚点和偏移，不能依靠运行时猜测透明边界。
- 角色逻辑画布固定为 128×128；每个正式动作使用一个透明 GIF，状态机按 `PetAction` 切换并重启对应 GIF。运行时可预解码和缓存纹理，但不得改变 GIF 作为正式动作交付格式。
- 登录页右侧固定使用“金币砸脑壳”循环装饰 GIF；缺失时降级为静态关键帧。

### 6.6 账号

- MVP 只使用用户名 + 密码注册登录，不提供邮箱、手机号、验证码或离线体验路径。
- MVP 不提供密码找回、恢复码或人工恢复原密码；注册时必须明确提示密码遗失会导致原账号无法恢复。
- 账号与计划数据以服务端为权威来源；本地存储只承担必要缓存和离线队列。
- 账号页不提供设备会话列表、数据导出、活动历史删除或“退出其他设备”；保留修改资料、修改密码、退出登录和删除账号。

## 7. 代码风格原则

- 简单优先。
- 外科手术式修改。
- 不做无关重构。
- 不添加未经要求的功能。
- 匹配现有代码风格。
- AI Provider、排程器、活动识别、经济账本和渲染层必须有明确边界。
- 正式状态、余额和购买必须由确定性代码及事务控制，不能直接信任 AI 输出或客户端余额。
- 不把高频输入事件直接映射为高频网络写入。
- UI、业务与平台系统能力必须分层；React/PixiJS 不得直接调用原生 macOS API，统一通过最小权限 preload 和平台适配层访问。

## 8. 测试与验证方式

工程建立后至少提供以下入口：

```bash
npm run lint
npm run typecheck
npm test
npm run build
npm run test:e2e
```

重点验证：

- 用户手动锁定与排序始终覆盖 AI。
- 创建任务后自动进入 AI 建议确认页，任务列表无逐条分析入口。
- 用户名密码注册登录不出现邮箱、手机号、验证码或离线体验路径。
- 同一排程输入和策略版本产生稳定结果。
- 键盘动作不采集按键内容，且本地反馈无明显延迟。
- 好友端只能收到泛化状态和聚合动作。
- 两个好友可见性设置相互独立；“不对其展示”只撤销单向投影且不解除好友。
- 活动识别页、桌宠菜单和菜单栏中不存在纠正入口。
- 午休、下班和加班倒扣完全依据用户设置。
- 购买账本原子扣减，重复请求不重复扣款。
- 多顶帽子的顺序、锚点、动作跟随和好友端显示一致。
- 128×128 透明 GIF 在预解码和纹理缓存后仍使用最近邻缩放；状态切换会从动作 GIF 首帧重新播放，非循环动作结束后按 Manifest fallback。
- DMG 在干净 Apple Silicon Mac 上可正常安装，应用签名、公证与 Gatekeeper 验证通过。
- Accessibility、Input Monitoring、Screen Recording 任一权限被拒绝时，对应识别能力正确降级且核心功能不受阻断。
- 菜单栏、登录项、Dock/Spaces/Stage Manager、透明窗口和自动更新必须取得真实 macOS 验证证据。

## 9. 当前已知风险

- 在线视觉识别可能看到敏感桌面内容，模型供应商、保留策略、排除应用和用户授权仍需在开发前冻结。
- “摸鱼”属于高误伤标签，必须使用更高置信度与持续时间门槛。
- 高频键盘输入若直接联网会造成消息洪泛，必须转换为 typing start、heartbeat、stop 等聚合事件。
- 窝囊费后台等价毫秒的离线预览和服务端权威计提之间存在同步取舍；正式余额以服务端账本为准。
- 用户仍可能填写异常时间；首版只允许同一自然日内且顺序合法的工时设置，但不得用桌面活动分类决定是否计提。
- 奖励池范围、无人合格时的滚存、5 分钟准点宽限、7 日 lot 过期和当日承诺任务快照已冻结；实现时必须保持事务幂等和先过期后分配的顺序，否则可能重复分配或被删除任务套利。
- 奖励池改为人民币原额汇总后，虚报高日薪并协同刷池的风险高于等价时间池；正式上线前需结合池范围、异常日薪和关联账号制定风控，但不得改变已确认的人民币原额分配规则。
- 帽子持续叠高会影响视口、遮挡和性能，需要纵向滚动、图层缓存和长列表事件载荷测试。
- GIF 正式运行时会带来解码、内存、透明边缘和动作切换首帧重启风险，必须验证预解码缓存、128×128 预算、fallback 与多角色性能。
- macOS 的 Accessibility、Input Monitoring 与 Screen Recording 权限相互独立，拒绝、撤销或系统设置变更后的状态恢复必须按真实系统行为验证。
- 全局输入聚合和窗口上下文可能需要带原生二进制的模块；必须验证 Electron ABI、Apple Silicon 架构、Hardened Runtime、签名和公证兼容性。
- 透明窗口在 Dock、Spaces、全屏应用和 Stage Manager 下的层级与点击穿透行为可能不同，不能只靠普通 BrowserWindow 测试推断。
- Developer ID 证书、Apple 公证、更新签名与发行凭据管理是正式发布门禁；未签名测试包不能视为可分发版本。
- 已有静态视觉稿和首个水豚动作资源原型，但仍没有完成运行时接入、可执行工程或真实联机验证；资源原型不能视为可运行产品原型。

## 10. 最近重要变更

- 2026-08-29：冻结中国大陆 MVP 产品口径：统一北京时间 `Asia/Shanghai`；App 打开后才计提；闪退/断线置灰，结算前重连恢复资格，结算时仍离线则当日所得转池；单人可获奖；池金额逐笔 7 日过期。
- 2026-08-29：在线截图识别产品开关改为默认开启但仍服从 macOS 系统授权；任务标题允许交给在线大模型分析，MVP 不使用跨用户匿名聚合基线。
- 2026-08-29：角色正式逻辑画布固定为 128×128，运行时动作契约改为 `PetAction` 切换透明 GIF；此前“GIF 仅评审、正式 Sprite Sheet/WebP Sheet”规则被替代。
- 2026-08-29：MVP 不提供密码找回或恢复机制；显示名可重复，好友采用个人好友视图，不建共享群房间；超高帽子塔直接纵向滚动。
- 2026-08-29：首发平台由 Windows 改为 macOS；Windows 延后。MVP 以 Apple Silicon `arm64` 为正式支持架构，通过官网分发 Developer ID 签名并经 Apple 公证的 DMG，Mac App Store 不进入 MVP。
- 2026-08-29：同步视觉评审确认的产品规则：新建待办自动进入 AI 建议确认；用户名密码登录；活动识别只读；取消拉黑并改为单向“不对其展示”；账号页移除设备与数据管理；好友活动广播和本机桌宠显示使用独立设置；登录页预留金币砸脑壳循环动画。
- 2026-08-29：金币砸角色场景改为角色无关的前景特效；金币采用原创经典平台游戏式竖向金色像素造型，只做直线下落，交付 8×1 透明 Sprite Strip，并在运行时对齐角色 `head` 锚点。此前将角色、桌子、受伤反应和金币合成在一起的版本只保留为探索稿。
- 2026-08-29：完成首个水豚“窝囊费金币砸脑门”历史动作原型；其 192×192 Sprite Sheet/GIF 只保留为探索证据，不符合随后确认的 128×128 正式 GIF 契约，正式接入前必须重导出。
- 2026-08-29：此前确认的 Windows 首发决定已被同日最新的 macOS 首发决定替代；预制角色与动作包、动画生成管线只负责资源生产的规则保持不变。
- 2026-08-29：桌面识别以准确度优先，可结合截图和在线视觉模型；好友只看泛化状态。
- 2026-08-29：任务录入由用户提供 DDL 和重要性，类型、时长与认知负荷由 AI 建议，用户决定最高优先。
- 2026-08-29：窝囊费只依据用户输入的日薪、上下班和午休时间，不根据桌面状态判断。
- 2026-08-29：虚拟办公室降级为简单排排坐工作界面；新增窝囊费商店、预制角色与可持续向上叠加且不设固定装备硬上限的帽子玩法。
- 2026-08-29：废止“窝囊时长是第二货币”的方案。用户只有窝囊费一种货币；商品以所需工作时长为基础价格，再乘用户单位时间窝囊费显示金额，确保不同工资用户购买力相同。
- 2026-08-29：废止“奖励池按等价时间均分”。首版只处理人民币；加班实际扣除的人民币窝囊费原额入池，多人按人民币分均分，高工资加班可为低工资获奖者带来更高购买力。
