# 不要加班开发进度

## 2026-W35

### 当前项目状态

- 阶段：产品口径已冻结；monorepo 工程骨架和 SiliconFlow 任务分析端点已存在，但桌面、经济、实时和新共享契约尚未形成可验收闭环。
- 当前分支：`codex/macos-prd-v2`。
- 状态来源：2026-08-29 的项目规则、PRD、Git 状态和实际文件检查。
- 已确认：当前工作区在本轮开始前干净，`HEAD` 与当时的 `origin/codex/macos-prd-v2` 均为 `cde99f5`。
- 并发变化：首次推送时发现远端已新增 `d8ba6d5`（monorepo 骨架）与 `83bce61`（SiliconFlow 任务分析端点）；已无冲突重放本轮 PRD 提交，并检查其关键合同与资产运行时代码。
- 待验证：Electron 及原生依赖尚未锁定，最低 macOS 版本仍不能正式确认。

### 本周目标

1. 将 2026-08-29 用户确认的中国大陆 MVP 产品决定同步到全部 PRD 契约。
2. 冻结北京时间、奖励池七日有效期、闪退重连结算、截图默认开启、标题大模型分析和 GIF 动作状态机规则。
3. 清理已经确认但仍标记为“待确认”的文档状态，保留必须由工程证据决定的未决项。

### Batch PRD-W35-01

并行判断：串行。经济、隐私、资产和实时状态均修改共享契约、数据字典、API 或统一待确认台账，共用文件范围明显重叠，不满足安全并行条件。

| Task ID | 目标 | 文件范围 | 依赖 | 状态 | Merge Gate |
|---|---|---|---|---|---|
| PRD-W35-T1 | 同步组合式入口、共享契约和产品总览 | 根 PRD、`00`、`01` | 用户确认 | 已完成 | Passed |
| PRD-W35-T2 | 冻结北京时间、工作会话、闪退重连和奖励池规则 | `03`、`13`、`14`、经济数据集 | T1 | 已完成 | Passed |
| PRD-W35-T3 | 冻结任务、AI、排程、活动、隐私规则 | `04`–`07`、`12` | T1 | 已完成 | Passed |
| PRD-W35-T4 | 冻结账号好友、排排坐、商店与 macOS 窗口规则 | `02`、`09`、`10`、`18` | T1 | 已完成 | Passed |
| PRD-W35-T5 | 把正式动作资产改为 128×128 GIF 状态切换契约 | `08`、`11`、Manifest 数据集 | T1 | 已完成 | Passed |
| PRD-W35-T6 | 同步路线、验收、待确认台账、项目规则和经验 | `15`–`17`、Agent、Lessons、README | T1–T5 | 已完成 | Passed |

### 本次计划

#### PRD-W35-T1

- 目标：将用户最新决定升级为跨模块权威规则。
- Todo：固定 `Asia/Shanghai`、中国大陆首发、确认项状态和 GIF 运行时方向。
- 验证：上位 PRD 与共享契约无冲突；旧“多时区/运行时禁用 GIF”表述可被全文检索定位并清理。

#### PRD-W35-T2

- 目标：定义只在 App 已打开时计提、断线置灰、结算前重连恢复资格、结算时仍断线则当日所得入池、单人领取及每笔七日过期。
- Todo：补齐状态、账本类型、池贡献来源、幂等和结算守恒字段。
- 验证：新增断线恢复、未重连转池、单人领取、七日过期和负余额测试场景。

#### PRD-W35-T3

- 目标：冻结承诺快照、50/10 排程、标题大模型分析、无跨用户基线、截图默认开启及中国区隐私策略。
- Todo：清除相应 `[待确认]` 标签；保留 macOS 系统授权和拒绝降级边界。
- 验证：标题只进入任务分析边界；截图未获系统授权时仍不得采集或上传。

#### PRD-W35-T4

- 目标：冻结显示名重复、100 好友/12 人渲染、个人好友视图、断线置灰、无独立隐身、商店范围、Dock/全屏策略。
- 验证：实时投影不泄露任务和经济信息；平台策略与桌宠模块一致。

#### PRD-W35-T5

- 目标：正式角色画布固定 128×128，一个 `PetAction` 对应一个透明 GIF，状态切换时切换并重启动作 GIF。
- Todo：Manifest、资源目录、预解码缓存、fallback、hash 和 QA 契约同步。
- 验证：JSON 样例可解析；全文不再把 Sprite Sheet/WebP Sheet 写成正式运行时唯一格式。

#### PRD-W35-T6

- 目标：让路线、验收、待确认台账和长期文档与新契约一致。
- 验证：JSON 全量解析；全文冲突搜索；`git status`、`git diff`、`git diff --stat` 检查无越界修改。

### 风险与取舍

- 正式运行时从 Sprite Sheet/WebP Sheet 改为 GIF，会增加解码、内存和首帧重启风险；通过预解码、纹理缓存和单角色资源预算控制。
- App 未打开不计提、结算时断线会把当日所得转池，必须使用服务端连接状态、事务和幂等键，避免误扣或重复入池。
- Screen Recording 产品开关默认开启不等于已获得 macOS 系统权限；未授权时必须保持无截图请求。
- 仅中国大陆首发仍需要上线前完成适用法律、隐私文本和在线模型供应商评审；本文档确认的是产品范围，不声称已取得法律意见。

### Todo

- [x] 完成 Batch PRD-W35-01 全部文档同步。
- [x] 完成 JSON、JSONL、Markdown 围栏、全文契约和 Git diff 验证。
- [x] 提交并推送 `codex/macos-prd-v2`。

### 分支更新

- PRD 内容提交：`08a139a docs: freeze China MVP product contracts`。
- 远端并发提交：先整合 `d8ba6d5` 与 `83bce61`，无文件冲突、未覆盖远端代码。
- 推送结果：已成功更新 GitHub 分支 `codex/macos-prd-v2`。

### 实际修改

- 入口与长期规则：同步中国大陆、北京时间、App 连接计提、奖励池、账号、权限、好友和 GIF 产品规则。
- 经济契约：增加 `DISCONNECTED` 会话状态、断线所得原子转池、单人领取、5 分钟截止、逐 lot 七日过期、先过期后分配、负余额及守恒字段。
- AI 与隐私：允许向已披露且无训练/无留存的在线模型发送当前任务标题；取消跨用户匿名基线；截图产品开关默认开启但仍服从 macOS 系统授权。
- 桌宠与内容：角色逻辑画布固定 128×128；每个 `PetAction` 使用透明 GIF；帽子塔直接纵向滚动；同步商店、Dock、全屏和资产许可证原则。
- 接口、数据与验收：更新 API、数据字典、Manifest、经济场景和 E-012 至 E-016 验收用例。

### 验证记录

- `git diff --check`：通过。
- `jq empty docs/prd/datasets/*.json`：通过，全部 JSON 样例可解析。
- 逐行解析 `docs/prd/datasets/*.jsonl`：通过。
- Markdown 代码围栏奇偶检查：通过，无未闭合围栏。
- 待确认项扫描：权威 PRD 仅保留 `PLATFORM-MIN-001`；AI 资产逐项许可证审计作为发布门禁保留。
- 旧契约扫描：正式 PRD 不再使用多时区、最小两人池、自动缩放帽子塔或 Sprite Sheet/WebP Sheet 正式动作规则；`design/pet-action-prototypes` 中的 192×192 产物仅为已标记的不合规历史原型。
- 本轮只修改文档和 JSON 示例。远端工程骨架在本轮末并发进入分支，未安装依赖，也未对他人提交执行 build、lint、unit 或 E2E；其可运行性和测试结果标记为待验证。

### 临时产物清理

- 本轮未创建构建目录、日志、截图、安装包或临时测试数据，无需清理。

### 文档同步

- `不要加班_agent.md`：已更新；原因是时区、经济结算、权限和动作格式均属于长期项目规则。
- `lessons.md`：已更新；记录 App 连接计提和历史 Sprite Sheet 契约被新 GIF 规则替代的长期防返工经验。

### Pending

- `PLATFORM-MIN-001`：锁定 Electron、Node 原生依赖和截图方案后复核最低 macOS 版本；当前仅以 macOS 13 为开发基线。
- AI 生成资产逐供应商、逐来源的具体商用许可证清单；当前只冻结“白名单 + 来源审计”原则。
- 并发落地的工程骨架尚未对齐本轮冻结契约：`packages/contracts` 仍使用开放字符串时区、旧 `PetAction`/presence；`packages/asset-runtime` 仍要求 Sprite Sheet；测试 fixture 仍含 `Asia/Taipei`。状态：待确认/待后续开发，不属于本次获批的 PRD 文档修改范围。
- SiliconFlow 任务分析端点已经把标题作为请求输入，但结构化校验、隐私日志边界、个人历史阈值和错误降级仍需按新 PRD 做代码审查与测试。状态：待确认。

### Blocked

- 无。
