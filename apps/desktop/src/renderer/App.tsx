import React from 'react';

export default function App() {
  return (
    <div className="app">
      <h1>不要加班 🐱</h1>
      <p>macOS 联机工作桌宠 — 把事做完，准点下班</p>
      {/* TODO: Router → Settings / Tasks / Schedule / Friends / Shop */}
    </div>
  );
}
